name: Ultra Simple Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: echo "No checkout needed"

      - name: Just echo
        run: |
          echo "Testing GitHub Actions"
          echo "If this works, problem is with other actions"